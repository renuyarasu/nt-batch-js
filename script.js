// Learn
console.clear();

// Array.concat()

let transport = ['🚐', '🚕', '🚋'];
let modernTransport = transport.concat(['🚀']);
let result = console.log(modernTransport); // [ '🚐', '🚕', '🚋', '🚀' ]
