// Learn
console.clear();

// Array.concat()

let transport = ['🚐', '🚕', '🚋'];
let modernTransport = ['🚐', '🚕', '🚋', '🚀'];
let lastAddedTransport = transport.at(-1);
let result = console.log(lastAddedTransport); // 
